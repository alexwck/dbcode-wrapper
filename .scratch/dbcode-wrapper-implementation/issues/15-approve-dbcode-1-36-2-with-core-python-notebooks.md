# 15 — Approve DBCode 1.36.2 with core Python notebooks

**What to build:** Promote DBCode `1.36.2` on the existing Code OSS `1.126.0` host and make Python cells in DBCode notebooks a normal, automatically prepared capability of DBCode Wrapper.

**Blocked by:** 14

**Type:** task

**Status:** resolved

- [x] Pin DBCode `1.36.2` from its verified Open VSX release and regenerate the canonical public-contribution digest without modifying or republishing DBCode content.
- [x] Treat the host and every external runtime extension as one candidate release set under test; verify exact metadata, engine compatibility, SHA-256, package size, publisher status, and Open VSX Ed25519 signature before installation.
- [x] Install the compatible Jupyter and Python runtime members needed for notebook execution automatically into the private profile. They are mandatory, not optional, and optional extension-pack members or unpinned dependencies must not be downloaded as a side effect.
- [x] Keep all external extensions outside the signed application bundle so the app bundle remains slim, but report their installed size separately.
- [x] Do not expose Extensions, Command Palette, Explorer, Chat, Run and Debug, or general Jupyter workbench routes merely because the runtime extensions are installed.
- [x] Prove a DBCode notebook still opens and renders, can use a Python cell, discovers a real Python kernel, executes a small deterministic expression, and shows the result.
- [x] Re-run the focused shell, licence, PostgreSQL, DuckDB, Parquet, persistence, package-size, signature, and exact-extension-inventory gates for the complete release set.
- [x] Keep a verified runnable previous app/profile snapshot and isolated preview path available until every gate passes; full installed-set replacement remains ticket 07.

## Comments

- 20 July 2026: Claimed after the official feature audit. The user explicitly chose Python notebooks as a core feature rather than an optional pinned package. The confirmed test seams are the release lock and package verifier, the exact private-profile extension inventory, and the rendered DBCode notebook workflow.
- 20 July 2026: The release lock now contains DBCode `1.36.2` and six mandatory Python/Jupyter packages. Every package passed its official stable Open VSX record, manifest, target-platform, size, SHA-256, verified-publisher, and Ed25519-signature checks, and its engine range was evaluated against Code OSS `1.126.0`, before the isolated profile accepted the exact seven-extension inventory.
- 20 July 2026: The signed app contains seven small built-ins, no source maps, and no external runtime extension. It measures `462,012 KiB` installed and `166,455,145` bytes in the indicative archive. The separate exact runtime profile measures `251,480 KiB`; DBCode itself accounts for `161,464 KiB` of that profile.
- 20 July 2026: The full rendered suite opened a real DBCode notebook, selected the isolated test Python kernel, handled DBCode's visible first-use Jupyter permission, rendered `DBCODE_NOTEBOOK_PYTHON_OUTPUT_42 42`, and proved the permission did not repeat. It also passed the focused shell, AI, Finder, Connections, Query Builder, diagram-route, SQL, table-grid, result-layout, relaunch, extension-inventory, and renderer-error checks without exposing a general IDE or Jupyter workbench.
- 20 July 2026: A proof-harness regression test was added after `prepare` exposed that an updated release identity could inherit old passed observations. A changed app or runtime set now clears completion, launches, quit state, and every manual check. The contaminated generated record was preserved separately, and the clean DBCode `1.36.2` proof starts with all five real-profile observations pending.
- 20 July 2026: Compatibility review found that the optional `ms-python.vscode-python-envs` pack member repeatedly tried to start a missing `pet` binary and then threw while shutting down. The focused runtime now omits that unused environment manager and the unused Python debugger, while keeping real DBCode Python execution. A clean rendered run produced `42`, proved the Jupyter permission does not repeat, and passed a new required-extension-host log audit with no unexpected errors.
- 20 July 2026: An ordinary launch can no longer install a candidate release set. Only the explicit rendered QA and real-profile proof workflows may prepare it; evidence calls the set “under test” until all gates promote it to approved.
- 20 July 2026: The slow real-profile start was waiting on macOS approval for `DBCode Wrapper Safe Storage`, not DBCode or Python initialization. The current ad-hoc build can prove that **Always Allow** does not repeat for an unchanged signed artifact; it cannot promise that trust survives a rebuild. The manifest now records that limitation explicitly, while ticket 08 requires a persistent local signing identity before same-Mac update acceptance.
- 20 July 2026: Review corrected an overclaim about rollback. The DBCode `1.36.1` source commit and package identities are recorded, but no isolated prior app/profile and rehearsed restore operation exist yet. This ticket therefore leaves rollback availability unchecked and ticket 07 remains responsible for the operational path.
- 21 July 2026: The user confirmed that macOS Safe Storage approval, rather than DBCode or Python initialization, accounts for the observed slow real-profile start. The app artifact remains unchanged for the next quit/relaunch check. The real profile still contains the two now-excluded optional Python pack members, so it must be normalized to the exact seven-extension candidate before the final database and persistence gate.
- 21 July 2026: The unchanged app reopened without another Safe Storage prompt, so same-artifact Keychain persistence passes. The real proof evidence is still bound to the earlier nine-extension inventory and must reset after the default profile is normalized to the exact seven-extension candidate; none of its old manual checks may be reused.
- 21 July 2026: The user completed the fresh exact-seven-extension proof after a full quit and relaunch. The lifetime account returned without licence re-entry or another Safe Storage prompt. PostgreSQL reported read-only mode and returned 3 rows totalling `75.00`; DuckDB and Parquet each returned 3 rows totalling `61.50`; the saved account, connections, files, and queries persisted. The proof harness finalized this evidence against DBCode `1.36.2` and the exact approved runtime inventory.
- 21 July 2026: A detached rebuild of the prior Code OSS `1.126.0` and DBCode `1.36.1` set passed signature, manifest, mock-Keychain launch, private-profile, and exact-inventory checks. Its runnable 605 MB app/profile snapshot is retained under ignored build data with digest verification and a disposable preview command. This closes the update-pair safety gate without claiming the full installed-set swap that remains ticket 07.
- 21 July 2026: The final source checks, static signed-host smoke, package-size audit, ordinary approved-profile preparation, and rollback verifier passed. The signed app is `462,012 KiB`, contains seven small built-ins and no source maps or external runtime extensions, and keeps the exact seven external extensions outside the bundle. DBCode `1.36.2` is now labelled approved with Code OSS `1.126.0`.

## Answer

DBCode `1.36.2` is approved on the pinned Code OSS `1.126.0` host with the exact seven-extension runtime set. Python notebooks are part of the normal product, while the optional debugger and broken environment-manager pack member remain excluded. Scripted development prepares this set before launch; a fresh Finder launch uses one focused verified setup action and never exposes a general extension surface. The complete rendered and real-profile gates pass, and a verified runnable DBCode `1.36.1` snapshot is retained for isolated rollback preview. Ticket 07 still owns actual installed-set promotion and restoration, and ticket 08 still owns a persistent signing identity for rebuild-time Keychain trust.
