# 09 — Publish public source and prepare the Private Personal Release

**What to build:** Publish a cleaned Public Source Repository, then turn the accepted same-Mac app into a versioned, host-only DMG for Macs owned by the licence holder. While the repository is public, use an authenticated GitHub draft release only as a temporary private transfer and never publish that release. Keep the current-user self-signed identity, include strong integrity evidence, and document macOS `Open Anyway` without weakening Gatekeeper globally. Each new Mac obtains the exact approved unchanged DBCode and Python/Jupyter packages separately from their official sources into its own current-user profile.

**Blocked by:** 08

**Human prerequisites:** GitHub authentication; a public GitHub repository; an explicit outbound-licence choice for the original wrapper code; approval of the clean public-history approach; and access to another owned Apple-silicon Mac

**Type:** task

**Status:** resolved

- [x] GitHub authentication is available without storing credentials in the repository, release assets, Actions logs, or ticket evidence.
- [x] The public source tree has a prominent unofficial-project disclaimer, an explicit original-code licence decision, Code OSS and VSCodium notices, and no copied DBCode manifest, VSIX, UI asset, website content, logo, profile, licence data, credential, database, signing secret, built app, or raw real-profile evidence. Sanitized version, digest, and pass or fail summaries may remain in issue notes.
- [x] The history selected for the first public push contains no old DBCode contribution snapshots, vendor screenshots, copied fonts, private development assets, personal paths, credentials, tokens, licence material, or signing secrets. The user's commit-email choice is reviewed before history is published.
- [x] `script/check_public_push_readiness.sh` passes against the exact ref intended for GitHub with the user's approved public email, private local account name, and SHA-256 of the exact outbound `LICENSE` file they selected. Its synthetic-history contract test remains part of `script/check_development.sh`; checking only the working tree is not accepted.
- [x] A public GitHub repository is created or linked only after the public-history review. Secret scanning and push protection are enabled, and every Actions workflow and log is treated as public data.
- [x] The current DBCode licence terms still allow the licence holder to use DBCode on their own devices. The repository and DMG do not bundle, mirror, modify, or redistribute the proprietary DBCode extension, a licence key, account state, database credentials, or a user profile.
- [x] The source tag, approved release lock, manifest, and packaged artifact identify the same release set. The final app retains Code OSS, VSCodium, Electron/Chromium, and dependency licence notices.
- [x] The app keeps its stable bundle identifier and persistent current-user self-signed identity. Every nested executable, helper, framework, and native component is signed from the inside out, the main app is signed last, and strict local verification passes without presenting the identity as Apple-trusted.
- [x] A read-only DMG contains only the exact signed host app and focused install guidance. Content scanning rejects DBCode, extension caches, profiles, global storage, licence or activation data, credentials, local databases, Keychain exports, and private keys. `hdiutil verify`, mounted-content hashing, and app signature verification all pass.
- [x] A versioned GitHub draft release contains the DMG, SHA-256 checksum, compatibility manifest, concise install and rollback notes, and a clear statement that Apple has neither identified nor notarized the app. The release remains `draft: true`, no automation can publish it, and its assets contain no signing secret.
- [x] An unauthenticated request cannot list the draft release or download its asset, while the repository owner can download it after authentication on another owned Mac. A published pre-release is not accepted as private.
- [x] The fresh owned Mac verifies the checksum before overriding any warning, moves the app to Applications, attempts one normal launch, and then uses only System Settings > Privacy & Security > Open Anyway. The process must not disable Gatekeeper globally or remove quarantine automatically.
- [x] If the approved DBCode or core Python/Jupyter packages are absent, first run offers one focused setup path that obtains and verifies the pinned packages from official sources. It does not expose a general extension marketplace or silently install a different version.
- [x] Licence activation, connection passwords, and other device-bound secrets are entered or imported intentionally on the new Mac and are never copied through GitHub.
- [x] The unchanged approved app relaunches without another Gatekeeper exception or Safe Storage prompt. Release notes state that each distinct downloaded host build may need `Open Anyway` and one new Safe Storage approval, while a DBCode-only external extension update does not change the host signature.
- [x] The actual package passes clean installation, first launch, offline relaunch after setup, PostgreSQL, DuckDB, Parquet, Python notebook, persistence, duplicate-install, and representative DBCode capability checks on an owned Mac other than the build Mac. Controlled upgrade and rollback remain same-Mac release-set gates and are not repeated on the second Mac.
- [x] The packaged app passes the complete DBCode Wrapper acceptance gate without claiming a published public app release, Apple malware scanning, Developer ID, notarization, Intel support, multi-user support, or official DBCode endorsement.

## Answer

The public wrapper source and the owner-only `v0.1.0` Private Personal Release are complete. The authenticated draft transfer remained private, the downloaded DMG matched its published checksum, and a fresh owned Apple-silicon Mac passed the documented manual Gatekeeper path, focused runtime setup, licence and protected-state persistence, PostgreSQL, DuckDB, Parquet, Python notebook, Kernel permission persistence, and duplicate installation.

The [official DBCode License Agreement](https://dbcode.io/legal/license-agreement), last updated 12 May 2026, grants the licensee a non-transferable right to install and use the extension on unlimited devices and gives lifetime-license holders continued access to updates. The wrapper stays within the recorded boundary: DBCode remains unchanged, external to the application and DMG, used only by the licence holder, and never transferred to a third party.

## Comments

- 2026-07-26: Final licence review used DBCode's official License Agreement, last updated 12 May 2026. It allows the licensee to install and use the extension on unlimited devices, includes future versions for a lifetime licence, and forbids third-party distribution. The accepted wrapper and DMG keep DBCode external and unchanged, transfer only the host between devices owned by the licensee, and therefore satisfy the remaining ticket gate without copying the vendor agreement into the public repository.
- 2026-07-25: The user accepted the completed second-Mac installation and feature evidence and removed second-Mac upgrade and rollback repetition from this ticket. Those behaviours remain mandatory same-Mac release-set gates for each new approved candidate; this decision removes a duplicate device test, not the controlled update or rollback contract.
- 2026-07-25: The fresh owned Mac followed the accepted Gatekeeper path: the checksum passed first, the app was placed in Applications, one normal launch was blocked, and the user opened it only through System Settings > Privacy & Security > Open Anyway. Gatekeeper was not disabled and quarantine was not removed automatically.
- 2026-07-25: Duplicate installation passed on the fresh owned Mac. After fully quitting, replacing the Applications copy from the exact same verified DMG, and relaunching, focused setup did not repeat, Gatekeeper and Safe Storage did not prompt, and the licence, connections, and saved work remained available through the unchanged external profile.
- 2026-07-25: After a full quit and relaunch of the unchanged app, a second real DBCode Python notebook ran with the same local kernel and the Kernel access prompt did not return. This completes the notebook execution and Kernel permission-persistence check on the fresh owned Mac.
- 2026-07-25: A real DBCode Python notebook passed on the fresh owned Mac using a locally selected kernel. The expected first-use Kernel access prompt appeared and was approved manually by the user; no automated step approved or bypassed it. Permission persistence remains to be checked with a second notebook launch.
- 2026-07-25: Parquet passed on the fresh owned Mac. DBCode read the disposable three-row Parquet fixture through DuckDB, returned the ordered rows in its result grid, and matched the expected `61.50` amount total.
- 2026-07-25: DuckDB passed on the fresh owned Mac. The unchanged DBCode connection route executed the versioned read-only proof and displayed its row in the DBCode result grid.
- 2026-07-25: PostgreSQL passed on the fresh owned Mac after the unchanged-app relaunch. DBCode executed the read-only proof and returned its result through the DBCode grid without another credential prompt.
- 2026-07-25: The unchanged `v0.1.0` app was fully quit and relaunched on the fresh owned Mac. The DBCode licence remained active, the protected connection worked without password re-entry, and neither Gatekeeper nor Safe Storage prompted again. This completes the device-bound secret entry and unchanged-app persistence gates without recording account, licence, connection, or Keychain contents.
- 2026-07-25: The title-row quick-input overlap was reproduced as a stacking defect in the maintained shell: pinned Code OSS gives quick inputs z-index `2550`, while the wrapper database toolbar used `2600`, so an expanded DBCode or wrapper picker could be painted underneath Connections. The focused patch now assigns quick inputs a wrapper-owned `2700` layer. A rendered regression forces the real overlap geometry and uses hit testing to prove the picker owns that region. The focused contract went red before the fix and green afterward; patch-plan integrity, rendered-test syntax, and the complete development source gate passed. This is a source fix only until a new host candidate is rebuilt and passes the built-host and rendered gates; the immutable `v0.1.0` transfer app is unchanged.
- 2026-07-25: On the fresh owned Mac, DBCode licence activation succeeded and an existing connection showed as active. No Safe Storage prompt appeared during activation. That absence is not treated as a failure by itself because no protected-state error occurred; the unchanged-app full-quit and relaunch gate still needs to prove that the licence and protected connection state persist without a repeated prompt. The same session exposed a separate focused-shell defect: expanding the title-bar search or command surface places it underneath the Connections navigation bar.
- 2026-07-25: The fresh owned Mac's focused setup installed the exact seven pinned DBCode and Python/Jupyter packages and requested one window reload. Before that reload, the focused shell attempted to write DBCode's result-location setting before the newly installed extension could register it, producing one non-fatal unregistered-configuration warning. The warning did not return after reload. Source diagnosis also confirmed that the toolbar's `DBCode active` label means the extension has activated; it does not inspect or claim licence activation. The wording is a UX ambiguity, while licence activation remains a separate pending human step.
- 2026-07-25: The repository owner downloaded the DMG and its checksum from the unpublished draft on another owned Apple-silicon Mac. `shasum -a 256 -c` returned `dmg: OK`. This completes the authenticated owner-download half of the privacy gate; the previously recorded anonymous listing, tag lookup, and direct-download denials complete its unauthenticated half. The DMG has not yet passed the target-Mac installation and Gatekeeper steps.
- 2026-07-24: The exact annotated `v0.1.0` source was rebuilt and accepted without changing the tag. App SHA-256 `effd272ba62b1559d890141371ba8d309ec44155c9fa0559977815c2cdd4aa39`, build-manifest SHA-256 `4977c3ef5f5278b10ed446eda1a7db5c53811f571e94933b64d70b65a7a8d3ea`, and release set `code-oss-1.126.0-dbcode-1.36.2-source-9892ef05f8db3317b9e201482b28ca6d4fc6fba0a7f5963c4a2e608e447636d5-artifact-effd272ba62b1559d890141371ba8d309ec44155c9fa0559977815c2cdd4aa39` agree. All 17 same-Mac gates, seven real-profile checks, 36 rendered checks, four independent compatibility pairings, two restart-health launches, and complete-set rollback passed with no failures or waivers. Raw profiles and evidence remain ignored local build output.
- 2026-07-24: The repository now uses one long-lived branch named `main`. The public root remains the tagged source commit, and the reviewed publication note was replayed as a new public-safe descendant. Earlier private development tips are recoverable through local-only `refs/private-archive/*` refs rather than branch names. Normal pushes are restricted to `refs/heads/main:refs/heads/main`; neither private archive refs nor tags are included implicitly.
- 2026-07-24: The reviewed public source is available at `https://github.com/alexwck/dbcode-wrapper`. Remote `main` contains the single parentless commit `48c309c95798869aae131f5815c4ce94a14f4d5e`, and annotated tag `v0.1.0` peels to that exact commit. Only those two refs were pushed. GitHub Issues and Wiki are disabled because local Markdown remains the project tracker; secret scanning and push protection are enabled.
- 2026-07-24: GitHub authentication was verified for `alexwck`. GitHub reported public account ID `36098488`, so the approved ID-based commit address is `36098488+alexwck@users.noreply.github.com`. The CLI credential remains in the macOS keyring and is not stored in the repository or ticket evidence. Repository-local Git configuration now uses the noreply address; global Git configuration was not changed.
- 2026-07-24: The owner explicitly approved an All Rights Reserved root licence for original wrapper code, use of the GitHub noreply commit address, and a separate one-commit public history that preserves the existing private development history locally.
- 2026-07-24: Final independent spec and standards reviews found no remaining defect after adversarial fixes. The focused setup now rejects extra external extensions, replacement public keys, streamed download overflow, HTTPS downgrade, excessive redirects, oversized, duplicated, or missing ZIP entries, and any package set that differs from the supplied strict Release Specification. The release scanner now rejects both credential columns such as `database_password` and row-oriented `key,value` credential records without rejecting ordinary Code OSS icon metadata. The complete source gate and task-level packager/verifier contracts pass.
- 2026-07-24: The first host-only QA DMG proved local creation, checksum, read-only mounting, content scanning, app digest, signature, and metadata verification, but review found that the accepted app could not obtain DBCode on a fresh Finder launch. That QA artifact is not a release candidate. The package gate now requires focused first-run runtime evidence in the signed manifest and rejects the current accepted artifact until it is rebuilt and fully reaccepted.
- 2026-07-24: Source implementation now gives a fresh Mac one explicit setup action for the exact seven pinned Open VSX packages. It verifies stable registry metadata, size, SHA-256, the embedded public key, Ed25519 signature, signature manifest, and VSIX manifest, installs with extension-pack dependencies disabled, checks the final managed inventory, and then offers one reload. Synthetic task-level tests exercise both the setup controller and the complete packager/verifier interfaces without reading or bundling the real DBCode package.
- 2026-07-24: Independent package review also tightened the source and privacy boundary. Final filenames include both source and app identity; an annotated tag must resolve to the manifest's source commit and exact release lock; the complete same-Mac gates, manual evidence, signing evidence, and evidence digests are required; and scans now reject extension caches, broader profile state, licence or activation records, credentials, Keychain exports, and signing material. Public licence, public commit email, clean-history approval, GitHub authentication, and the second owned Mac remain human gates.
- 2026-07-24: Claimed after the same-Mac release passed final acceptance. Work starts with the reversible local gates: inspect the exact public source tree and reachable history, verify the outbound licence and Git identity prerequisites, and build a host-only DMG contract. No GitHub repository, push, draft release, or asset upload may occur until the exact outbound ref passes the public-readiness gate and the remaining human choices are explicit.
- 2026-07-22: The user chose a Private Personal Release so the app can be installed on other devices they own, without distributing DBCode or personal data. DBCode remains external and is installed from its official source on each device.
- 2026-07-22: The user selected the unpaid self-signed route and explicitly removed paid Apple Developer Program membership, Developer ID, and notarization from future consideration. Manual `Open Anyway` approval and the known per-host-build Safe Storage prompt are accepted product constraints, not temporary placeholders.
- 2026-07-22: The user decided that the GitHub source repository will be public for now. Official GitHub documentation says published assets in a public repository are public, while draft releases are visible only to users with push access. The plan therefore separates a cleaned public source repository from an authenticated draft transfer that must never be published. The first public push remains blocked on an outbound-licence choice and explicit approval of the clean-history approach.
- 2026-07-22: The public-source preparation now has separate gates for the current source tree and the exact Git ref intended for publication. The history gate checks deleted forbidden files, private-key and common live-token patterns, the owner's absolute personal home path, third-party notices, the SHA-256 of whichever outbound `LICENSE` file the owner selects, and every reachable author and committer email.
- 2026-07-24: The final host-only package contains exactly five private-transfer assets. Its read-only HFS+ DMG is 187,844,243 bytes with SHA-256 `9650d3fc97332657da16486195f43d75562187273b100ce4b66fcfda3ee9bedd`. The package verifier matched annotated tag `v0.1.0`, source revision `48c309c95798869aae131f5815c4ce94a14f4d5e`, the approved release set, the accepted app digest, all required notices, the complete same-Mac acceptance report, mounted contents, and strict nested signatures. Host-only scanning and sanitized metadata checks passed; DBCode, extension caches, profiles, licence state, credentials, databases, Keychain material, and signing secrets are absent. Packaging also exposed and fixed a relative-app-path defect in the focused runtime validator; the task-level contract now covers both absolute and relative application paths.
- 2026-07-24: The authenticated `v0.1.0` GitHub release is an unpublished draft with no publication timestamp. Its five uploaded assets match the local filenames, byte sizes, and SHA-256 digests, and an authenticated owner checksum download matched byte-for-byte. A clean anonymous API listing omitted the draft, anonymous lookup by tag returned 404, and direct anonymous asset download returned 404. The repository has no Actions workflows that can publish the draft. Remote refs remain one `main` branch plus the unchanged annotated `v0.1.0` tag. Download and installation from the other owned Mac remain open.
- 2026-07-27: Future private packages may use prompt-free acceptance schema 3. It binds the exact signed app and release lock to the development gate, static smoke, and one persistent-profile rendered smoke without manual evidence. The existing `v0.1.0` same-Mac acceptance remains valid historical evidence, and older schemas remain readable for compatibility.
