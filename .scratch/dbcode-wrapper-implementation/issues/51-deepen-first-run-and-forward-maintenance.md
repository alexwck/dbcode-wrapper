# 51 — Deepen first-run and forward maintenance

**What to build:** Make every visible first-run command reachable in every activation phase, remove stale maintenance remnants, close generated-path and public-source gaps, and deepen the focused-shell and slimming seams without changing DBCode-owned behaviour or slowing the prompt-free release path.

**Blocked by:** none

**Type:** task

**Status:** resolved

- [x] Register Runtime Setup and Profile Setup before selecting an activation phase, and route an unmet prerequisite without producing `command not found`.
- [x] Share first-run webview escaping, nonce, CSP, and action-message policy behind one wrapper module.
- [x] Remove the unused encoded DuckDB fixture, unlinked superseded research, and no-caller shell exports.
- [x] Add the narrow encoded-DuckDB ignore and public-source rejection rule.
- [x] Make cached patch verification remove its temporary Git index on success and failure, then expire the exact leaked path safely.
- [x] Separate active slimming choices from dated measurement evidence.
- [x] Give the wholly wrapper-owned focused-shell TypeScript and CSS first-class authoring files while preserving the exact prepared tree.
- [x] Keep resolved issues historical and current public guidance forward-facing.
- [x] Run focused contracts, the complete prompt-free development gate, and proportionate prepared-tree or rendered verification.
- [x] Refresh the affected wiki pages from the committed implementation and audit their links.

## Answer

The wrapper now registers Runtime Setup and Profile Setup as soon as it starts. Profile Setup routes to Runtime Setup when required packages are missing, so the visible action no longer depends on a later activation phase. Both first-run views use one fail-closed webview safety module.

Maintenance is smaller and safer. The unused encoded DuckDB fixture, four unlinked research notes, one unused shell library, and unused exports are gone. The narrow `*.duckdb.gz.base64` ignore and public-source rule prevent that generated fixture form from returning. Cached patch verification owns and removes its temporary Git index on success and failure; Generated Workspace Retention removed the one exact leaked 2,253,037-byte index.

Active slimming choices now stay in policy while the complete dated measurements stay in a linked evidence document. Wrapper-owned focused-shell TypeScript and CSS are first-class source files. The build materializes and verifies them before compilation, and their prepared-tree bytes and digest did not change.

Public guidance is forward-facing. Resolved issues remain history, the affected wiki pages are anchored to commit `5f77cbeeb00b79432ca86b95b0d392d68f0d1d27`, and the 32-page wiki has no lint findings or dead wiki links. The routine source gate remains prompt-free and completed in under half a minute without a build, app launch, network request, or human intervention.

## Comments

- 2026-07-30: Claimed after the user approved every recommendation from the forward architecture and cleanup review. The source gate passed in 25.87 seconds before implementation, with no build, app launch, network request, or human prompt.
- 2026-07-30: The final prompt-free development gate passed without rebuilding the app. One Host Session process-table fixture was skipped because the current sandbox does not permit that check; the maintained gate still completed successfully.
- 2026-07-30: The canonical focused-shell TypeScript and CSS matched the retained prepared Code OSS tree byte for byte, and `verify_prepared_patch_tree.sh` accepted its unchanged `601ece171404df0fa8fd336a97697cab818d88e6b9473ed3f36abbf0a87eb14f` digest.
- 2026-07-30: Generated Workspace Retention removed the exact 2,253,037-byte leaked `.build/code-oss-patch-index.iLIaaN` after a dry run. It was rebuildable temporary test output and is not recoverable. The following inventory contained no unknown or expired paths.
- 2026-07-30: Two-axis review found no scope creep. Its cleanup-path, materializer-path, trusted-webview-input, and lossless-measurement findings were fixed before the final gate.
- 2026-07-30: The refreshed 32-page wiki linted with zero errors and warnings, its wiki-only link audit found no dead links, and the live overview preview loaded the new guidance from source commit `5f77cbeeb00b79432ca86b95b0d392d68f0d1d27`.
