# 38 — Approve DBCode 1.36.4 and hold Code OSS 1.130.0 safely

**What to build:** Form and test one exact candidate release set using DBCode `1.36.4` on the proven Code OSS `1.126.0` and VSCodium `1.126.04524` host. Keep Code OSS `1.130.0` visible as a separate untested update until official compatible VSCodium packaging exists. Approve, install, and roll back only a complete tested set.

**Blocked by:** none

**Type:** task

**Status:** resolved

- [x] Official stable metadata is captured from Microsoft's Code OSS GitHub release, VSCodium's GitHub release, DBCode's official changelog, and DBCode's verified stable Open VSX record.
- [x] The Release Specification pins exact immutable tags, commits, publication dates, release-note routes, runtime versions, package metadata, sizes, SHA-256 values, public-key identity, signature archive digest, and public contribution digest.
- [x] VSCodium remains at `1.126.04524`. Its official stable and master metadata still target Code OSS `1.126.0`, and its patch stack demonstrably fails against `1.130.0`. Code OSS `1.130.0` therefore remains **Not tested** and is not mixed into this update.
- [x] DBCode `1.36.4` remains unchanged and external. Its complete connection catalogue and public contribution contract are compared with the approved baseline; new capabilities, including the database-routine debugger, are retained unless a documented compatibility gap proves otherwise.
- [x] The semantic Code OSS patch plan applies cleanly to the retained `1.126.0` host, and focused shell, profile, release-status, slimming, package-verification, and first-run setup contracts pass before a full build starts.
- [x] The rebuilt candidate app passes static host smoke, the one-profile prompt-free rendered smoke, exact extension inventory, package signatures, size, signing, and prompt-free release acceptance.
- [x] No second-Mac upgrade or rollback repetition is required. A second-Mac install is needed again only if the device-install contract itself changes.
- [x] Only the fully accepted release set can enter approved history or replace the installed app. Failure at any gate leaves the current approved set and rollback evidence intact.

## Comments

- 2026-07-25: Official discovery returned Code OSS `1.130.0` published on 22 July, VSCodium `1.126.04524` as the still-current stable packaging release, and verified stable DBCode `1.36.4` published on 24 July. This is a candidate tuple, not an approved update.
- 2026-07-25: Code OSS tag `1.130.0` resolves to commit `1b6a188127eeaf9194f945eb6eb89a657e93c54c` and declares Electron `42.6.0`. DBCode `1.36.4` keeps engine `^1.95.0`, uses the existing verified Open VSX public key, and has an official versioned changelog page. Exact package and contribution evidence must pass the repository verifier before the release lock is promoted.
- 2026-07-25: A real source-preparation check proved that VSCodium `1.126.04524` cannot safely package Code OSS `1.130.0`: the official VSCodium `00-brand-remove-branding.patch` already fails against the newer Code OSS tree. VSCodium master commit `c7ffd77bb58cf3423fb6d40258f22c1156dd6505` still pins Code OSS `1.126.0`, and no official `1.130` tag exists. The candidate was narrowed to DBCode `1.36.4` on the approved host instead of bypassing the complete-release-set contract.
- 2026-07-25: DBCode `1.36.4` changes its public contribution surface: it adds `dbcode.debug.routine`, the `dbcode` debugger type, and SQL breakpoints. The feature policy now treats that as an explicit candidate capability requiring a focused debugger validation rather than silently hiding it.
- 2026-07-26: The debugger gate now has its own local PostgreSQL fixture instead of changing the existing read-only proof. The fixture pins PostgreSQL `17.4` Bookworm by immutable manifest, pins `postgresql-17-pldebugger` `1:1.10-1.pgdg12+1`, preloads `plugin_debugger`, installs `pldbgapi`, and gives a non-superuser local role ownership of a deterministic PL/pgSQL routine. The first and repeated SQL-level preparations passed with the same local image identity: `pldbgapi` reported `1.1`, both the administrator and routine owner returned `22`, and no authentication secret entered source or evidence. The optional live contract passed through the same `prepare-debugger` interface. Finalization now requires the exact sanitized loopback, image, package, extension, routine-owner, and result evidence as well as the debugger observation; the canonical check list lives in one proof-state module to prevent future gate drift. The human DBCode breakpoint/stepping check remains required before approval.
- 2026-07-27: The feature policy no longer treats all AI as one supported feature. It now records provider and inline completion, Query Builder AI, Grid AI, Explore and plan analysis, Copilot tools, MCP, inferred relationships, and team controls separately. The policy keeps live models, account sign-in, OAuth, DML, DDL, data copy, and workspace writes outside default CI. Explore and plan-analysis routes still need focused rendered evidence; Copilot tools remain limited because the generic Chat host is intentionally absent; HTTP MCP remains limited until its lifecycle, OAuth approval, and one external client pass.
- 2026-07-27: The AI policy now gives every feature its own `declared`, `reachable`, `rendered`, or `live` evidence list. Provider configuration is rendered. Inline completion, Query Builder AI, Grid AI, Explore AI, and plan analysis remain candidate validations instead of inheriting provider-settings evidence. Query explanations, Copilot tools, HTTP MCP, inferred-relationship writes, and team controls remain explicitly limited. An approved policy cannot retain a `requires-validation` feature.
- 2026-07-27: Automatic MCP registration and the optional HTTP MCP server now have separate policy records. Automatic registration is supported and reachable through DBCode's unchanged provider. For HTTP MCP, only the settings page is reachable; Start, Stop, token revocation, OAuth, and an external client remain declared but unproved. The source gate locks that distinction without starting a server or requiring a client.
- 2026-07-27: The user removed human-required tests from deployment. The candidate no longer waits for Kernel, Keychain, licence, debugger, database, model, OAuth, or other person-controlled proof. The normal gate now uses one persistent generated `qa` profile, route-level rendered evidence, and prompt-free schema 3 release acceptance. The PostgreSQL debugger fixture and older real-profile proof remain optional diagnostics; they do not block packaging. Earlier comments describe the superseded acceptance model.
- 2026-07-27: The unattended rendered smoke passed in the persistent `qa` profile against DBCode `1.36.4`. It verified the exact 12-section, 88-item New Connection fingerprint, the focused shell, retained notebook, Query Builder, settings, and AI routes, Connections Home, persistent Database Explorer, release-status picker layering and dismissal, safe SQL-file opening, temporary Account dismissal, and current extension-host logs. It did not accept DBCode terms or start a kernel, database, debugger, model, secret, or macOS prompt. A rebuilt app and final schema-3 acceptance report still remain.
- 2026-07-27: The final default source gate passed without launching the app or using a person-controlled step. Static smoke no longer launches a second disposable profile; the rendered `qa` profile is the only automated GUI profile. Deep rollback, controlled-upgrade, debugger-fixture, proof-state, and older same-Mac acceptance suites are now run only by changes that own those workflows. The default source gate completes in about 30 seconds on this machine.
- 2026-07-27: Final prompt-free acceptance passed for source `db6e2bf14ed29a609fb4bc5d36e3dfaa01217c17`, DBCode `1.36.4`, Code OSS `1.126.0`, and VSCodium `1.126.04524`. The signed app digest is `0136b228242c886d0d4010f273d9d0c21eb17dccc6411a7d0379317cf2cbb8cd`. Static smoke passed in 4.83 seconds, all 13 one-profile rendered checks passed in 14.88 seconds, and exact-source schema-3 acceptance passed in 47.44 seconds with no failures or waivers. The gate did not start a kernel, execute SQL, call a model, enter a secret, or invoke a person-controlled action. The candidate is fully tested but has not entered approved history or replaced the installed app, so the final promotion criterion remains open. Code OSS `1.130.0` remains separate and **Not tested**.
- 2026-07-27: Annotated tag `v0.1.1` now identifies the exact accepted source `db6e2bf14ed29a609fb4bc5d36e3dfaa01217c17`. Host-only packaging completed in 65.92 seconds and produced exactly five owner-only assets. The read-only DMG is 187,900,484 bytes with SHA-256 `ee03bd16ca2f4bc09f502206758ad283d447156b82e5754d83e0393497c01254`. Its external checksum passed, the mounted tree contained only the signed app and install guide, and all 14 package checks passed with no failure. A separate 27.70-second verification of the final moved assets produced the same receipt apart from its timestamp. The tag and assets remain local: they have not been pushed, uploaded, published, installed, or entered into approved history, so the final promotion criterion remains open.
- 2026-07-27: The public `main` branch now ends at `477da857cfb57126890263779e9dc7dc4513b9cb`, and the remote annotated `v0.1.1` tag peels to the accepted source `db6e2bf14ed29a609fb4bc5d36e3dfaa01217c17`. Both exact refs passed the public-push readiness gate before transfer. The authenticated owner created an unpublished GitHub draft with `draft: true`, no publication timestamp, and exactly the five verified assets. GitHub's recorded sizes and SHA-256 digests match the local files, and an authenticated download matched all five files byte for byte. Without credentials, the public release list omits `v0.1.1`, tag lookup and direct DMG download both return `404`, and the repository reports zero Actions workflows. Nothing was published or installed, and approved history is unchanged, so the final promotion criterion remains open.
- 2026-07-27: Prompt-free approval now has one central writer. Its adapter uses the existing Release Specification and Private Personal Release validators for the complete lock and schema-3 acceptance report, then the writer binds their purpose records to the runtime inventories, compatibility record, final package receipt, exact confirmation, and no-install attestation. The package verifier reads the same canonical check list. Public-writer rejection tests cover incomplete acceptance, incomplete signing policy, mismatched versions, writable media, stale evidence, missing checks, and any attestation that claims installation. The full development gate passed in about 18 seconds without rebuilding or launching the app; its only skip was the existing sandbox-only process-table fixture. This validates the promotion mechanism but does not yet promote the real candidate, so the final criterion remains open.
- 2026-07-27: Prompt-free approval now consumes the final mounted-package verification receipt instead of repeating live `codesign` and `lipo` checks against the build app. The focused contract passed while those signature tools were forced to fail if approval invoked them, proving that packaging and mounted verification remain the only live signature gates. The full development gate passed in about 17 seconds, with only the existing sandbox-only process-table fixture skipped; the public source-tree contract and whitespace check also passed. No app was built, launched, installed, or written into the production profile.
- 2026-07-27: The exact `v0.1.1` release set entered maintained approved history through the prompt-free approval bundle created at `2026-07-27T17:07:14Z`. Its record binds source `db6e2bf14ed29a609fb4bc5d36e3dfaa01217c17`, app SHA-256 `0136b228242c886d0d4010f273d9d0c21eb17dccc6411a7d0379317cf2cbb8cd`, the accepted package evidence, and DBCode `1.36.4` on Code OSS `1.126.0`. The maintained feature policy is now approved and has no pending `requires-validation` state: capabilities outside the prompt-free evidence are recorded as limited instead of being falsely presented as tested. The focused policy and runtime contracts passed, and the final full development gate passed in about 21 seconds with only the existing sandbox-only process-table fixture skipped; its public source-tree check also passed. Approval did not install the app or write the production profile. Code OSS `1.130.0` remains separate and **Not tested**.

## Answer

DBCode `1.36.4` is approved on the retained Code OSS `1.126.0` and VSCodium `1.126.04524` host as exact private release `v0.1.1`. The approved record comes from the accepted source, signed app, prompt-free acceptance report, read-only DMG, and independent mounted-package verification.

Approval and installation remain separate. This task added the exact release set to maintained approved history without replacing the installed app, changing the production profile, accepting a macOS prompt, or running a database, kernel, debugger, or AI model. Code OSS `1.130.0` remains visible as **Not tested** until compatible VSCodium packaging exists.
