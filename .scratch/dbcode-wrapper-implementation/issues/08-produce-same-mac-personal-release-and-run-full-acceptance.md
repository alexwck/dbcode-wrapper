# 08 — Produce the same-Mac personal release and run full acceptance

**What to build:** Produce the first usable private DBCode Wrapper app for the current Mac and prove that the complete product—not just its parts—satisfies the approved identity, interface, licensing, database, migration, update, rollback, and security contract.

**Blocked by:** 05, 07, 22, 23

**Type:** task

**Status:** resolved

- [x] The release is an independently launchable Apple-silicon application with the approved original identity and persistent current-user self-signed identity. The unchanged artifact must not repeat its Safe Storage prompt after approval; a distinct rebuilt host may require one new approval, and the release must state that limitation instead of claiming prompt-free host updates.
- [x] The production build has no diagnostic escape into a general workbench and exposes only DBCode functionality through the approved database-client redesign.
- [x] The exact unchanged DBCode package remains the only database-client product extension. Its approved Python/Jupyter runtime dependencies are exact, external to the signed bundle, and do not expose generic IDE routes; DBCode activates normally and retains the lifetime Pro entitlement after relaunch.
- [x] PostgreSQL, DuckDB, Parquet, first-run migration, protected-credential re-entry, conditional hyphen-path handling, profile isolation, and full persistence pass in the packaged application.
- [x] Update discovery, candidate testing, explicit promotion, restart health checking, and a complete rollback rehearsal pass using real Approved Release Set manifests.
- [x] Strict signature verification passes, the app bundle remains unchanged after use, and current-user-only permissions protect executable extension and profile directories.
- [x] The full acceptance report records every gate, exact artifact identity and digest, manual evidence, failures or waivers, and the still-applicable private-use risks.
- [x] The release does not claim public distribution readiness, Developer ID identity, notarization, Intel support, multi-user support, or official DBCode endorsement. Paid Apple distribution features are out of scope rather than deferred release work.

## Comments

- 20 July 2026: Real-profile startup exposed the limit of the ad-hoc checkpoint: macOS can ask again for `DBCode Wrapper Safe Storage` access after a rebuild. The unchanged-artifact quit/relaunch gate remains valid, but same-Mac update acceptance now requires a persistent local certificate. The later Private Personal Release decision below closes the earlier public-distribution branch.
- 21 July 2026: The user confirmed that the visible Safe Storage approval is the source of the slow real-profile start. Startup timing must exclude time spent waiting for that human Keychain decision. The current artifact was not rebuilt or re-signed before the next quit/relaunch check; a repeated prompt on that unchanged artifact is a failure, while prompts after ad-hoc rebuilds remain expected until persistent local signing is implemented.
- 21 July 2026: The user quit and reopened the unchanged signed app and confirmed that the Safe Storage prompt did not return. Same-artifact Keychain persistence therefore passes. This does not yet satisfy the rebuilt-update criterion, which remains dependent on a persistent local signing certificate.
- 22 July 2026: Claimed for implementation. The first gate is to identify the current signature and establish a stable, current-user-only local signing identity before rebuilding the final same-Mac release. This remains a private-use identity and does not imply Developer ID or notarization.
- 22 July 2026: The persistent self-signed certificate passed strict signature checks across two distinct artifacts, but the real Safe Storage update check failed: rebuild A prompted once and rebuild B prompted again. The Keychain ACL records each build's changing `cdhash`, while both signatures report `TeamIdentifier=not set`. Cryptographic identity continuity alone is therefore not enough to claim prompt-free Safe Storage access after a Code OSS host rebuild. DBCode-only external extension updates do not change the app signature; host rebuilds need either an Apple-issued signing identity with a stable Team ID or an explicitly accepted one-time Keychain approval/update step.
- 22 July 2026: The user selected the Private Personal Release route and explicitly removed paid Developer ID signing and notarization from future consideration. The accepted contract is manual macOS approval on each owned Mac, no repeat for the unchanged artifact, and an honestly documented possible approval after each distinct host rebuild. This supersedes the earlier ticket 09 public-distribution branch without changing the recorded test evidence.
- 22 July 2026: The signing receipt, build manifest, smoke check, and final acceptance report now separate stable cryptographic identity from unstable Safe Storage access across rebuilt hosts. The real A-to-B observation is recorded as an accepted new approval after a distinct rebuild, and stale receipts are rejected unless both the artifact digest and complete release-set ID match. Source contracts and the isolated signed-app smoke pass. This ticket remains claimed until the current rebuilt artifact receives fresh real-profile activation, protected-credential re-entry, update discovery, database, persistence, compatibility, restart-health, and rollback evidence.
- 23 July 2026: Acceptance now waits for the approved architecture program in tickets 16–22. Those changes intentionally alter wrapper sources, patch identity, and the candidate release set, so certifying the current artifact first would create evidence that cannot be reused. PostgreSQL, DuckDB, and Parquet remain representative live fixtures; they are not the complete DBCode connection-support boundary.
- 23 July 2026: The semantic patch-plan migration changed the maintained patch digest to `fa7caac028cd56fff3aa6ed3fe347dfd03050397f75154dd3ea17d39e5e10866` and created candidate source identity `code-oss-1.126.0-dbcode-1.36.2-source-097d2247d6042a654e2c8a73dcaa3d0cc8f758fbf66affa818b39b16308a7151`. The existing app, manifest, proof receipt, automated-gate receipt, approval attestation, and acceptance observations must not be promoted as evidence for that new source identity. Ticket 22 and the remaining architecture verification will change it again before the final candidate is built.
- 23 July 2026: Tickets 16–22 are now resolved and the complete architecture source suite passes. The final candidate source identity is `code-oss-1.126.0-dbcode-1.36.2-source-3def4f2dba78c29952fc65375053ffcbae465f99ffb623ab29495128571e987a`; its wrapper-source digest is `4df54dfe5d0f7682493072d6cfa35a700d6f8325e754faa26c95af28e7077ee9` and its semantic patch-plan digest remains `fa7caac028cd56fff3aa6ed3fe347dfd03050397f75154dd3ea17d39e5e10866`. The existing signed app passed the complete 36-check rendered behaviour proof, including the unchanged 88-choice connection catalogue, but it is not an artifact of this final source identity. The next step is one clean signed rebuild followed by fresh ticket 08 acceptance evidence.
- 23 July 2026: The clean signed candidate `a1aaa04ab32ab815dd27679e3651de698afb351fed9895e41f0b4d16d64b8c56` passed strict signing continuity, isolated smoke, all 36 rendered checks, the 88-choice connection catalogue, lifetime activation, unchanged-artifact Safe Storage behavior, protected PostgreSQL credential re-entry, PostgreSQL, DuckDB, Parquet, and persistence. Update discovery failed: its forced network check saw current VSCodium and DBCode records but omitted the independently published Code OSS `1.130.0` record. Ticket 23 now blocks finalization; the failed proof remains preserved rather than being promoted.
- 24 July 2026: Tickets 23–27 closed the remaining update-discovery, official-link, frozen-rollback, four-way compatibility, and deterministic proof-inventory gaps. The full development suite passes from source revision `5ed51087008570db4afb046f3c78c10ae0cd91a8`.
- 24 July 2026: Final acceptance passed for `dist/DBCode Wrapper.app`, SHA-256 `06fe1ac3ad3e200cb12effa0b7536c7fdd9bddf63d0b36d64b1f01d51cbd59e2`, release set `code-oss-1.126.0-dbcode-1.36.2-source-6b9ab1d761aed668f3209c0a447c3f5103a7cb098108addffec7ef74701ee5ad-artifact-06fe1ac3ad3e200cb12effa0b7536c7fdd9bddf63d0b36d64b1f01d51cbd59e2`, and build-manifest SHA-256 `fc9f000933048dae67371551ae4373a7ff987f44bb99e9229b6224c6a9cc212d`.
- 24 July 2026: The final report records 17 passed gates, all seven passed real-profile observations, 36 rendered checks, zero unexpected rendered errors, two clean real-Keychain restart-health launches, all four compatibility pairings, a complete-set rollback, no failures, and no waivers. The app was fully closed after the gates.
- 24 July 2026: The accepted limits remain explicit: this is an Apple-silicon, current-user Private Personal Release with a self-signed local certificate. It is not Developer ID signed, notarized, ready for public app distribution, multi-user capable, or an official DBCode product. A distinct host build may require `Open Anyway` and one new Safe Storage approval; an unchanged artifact must not repeat them.

## Answer

The same-Mac DBCode Wrapper release is accepted. It runs the unchanged licensed DBCode `1.36.2` extension and the exact core Python/Jupyter set on Code OSS `1.126.0` as a focused database application. Its signed app, isolated profile, lifetime activation, protected credentials, representative database workflows, update controls, restart health, rollback, and private-use limits all passed the final acceptance gate.
