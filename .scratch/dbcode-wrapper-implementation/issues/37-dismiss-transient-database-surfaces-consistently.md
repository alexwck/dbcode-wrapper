# 37 — Dismiss transient database surfaces consistently

**What to build:** Make DBCode Wrapper's temporary pickers, menus, and DBCode-only drawers behave like one coherent macOS database application. Clicking elsewhere closes the active temporary surface, Escape remains available, and interacting inside the surface does not dismiss it. Treat Database Explorer as persistent workspace navigation: editor, canvas, result-grid, and Escape interactions keep it open. Only its direct toolbar action toggles it closed; another DBCode action may replace it when that action uses the same sidebar. Keep the top-level Connections card view as the single primary connection entry and remove only the duplicate Connections Home item from its adjacent tools menu. Keep DBCode's provider workflow unchanged while giving Custom Model setup a clear focused route.

**Blocked by:** none

**Type:** task

**Status:** resolved

- [x] A real Code OSS quick-input workflow stays above the title toolbar, dismisses on an outside click or Escape, stays open for clicks inside it, and returns focus without activating a hidden generic IDE surface.
- [x] Wrapper context menus dismiss on an outside click and remain usable for clicks inside them.
- [x] Database Explorer stays open when the user clicks the query canvas or a DBCode result grid or presses Escape, stays usable during those interactions, and closes only through its direct toolbar action or replacement by another DBCode action using the same sidebar.
- [x] Every temporary DBCode-only sidebar drawer, including Account, Tunnels, Authentication Profiles, Streams, History, and Library, uses the same outside-click, Escape, inside-interaction, and result-grid focus rules.
- [x] Repeatedly pressing a direct drawer action toggles that drawer closed, while choosing a different drawer changes the visible DBCode view without exposing unrelated sidebar sections.
- [x] The Connections button remains the sole primary route to DBCode's card-based connection entry. The adjacent connection-tools menu no longer repeats Connections Home, while New Connection, Import, Sample, SQL file, Tunnels, Authentication Profiles, conditional Streams, and Profile Setup remain reachable through their retained routes.
- [x] DBCode Tools explains the provider paths in product language: DBCode AI needs no extra key screen, Custom Model has a focused settings route for endpoint and model, and the DBCode-owned API-key command remains available. No generic settings or extension marketplace is exposed.
- [x] Provider, Custom Model, and API-key routes remain reachable without choosing a provider, accepting terms, entering a secret, or calling a model during deployment.
- [x] Focused source tests cover the shared dismissal and focus rules. The approved one-profile smoke renders representative quick-input, Database Explorer, Connections Home, Account, connection, and AI-route behaviour without modifying the DBCode package.

## Comments

- 2026-07-25: The user reported that a title-bar or DBCode picker can remain open after clicking elsewhere, Account and other DBCode drawers lack an outside-click dismissal, the connection-tools menu repeats Connections Home, and choosing DBCode AI appears to do nothing. The supplied import-source screenshot confirms the affected picker is the real Code OSS quick-input surface used by DBCode.
- 2026-07-25: Official DBCode documentation defines three provider paths: Custom Model, GitHub Copilot when separately installed and active, and DBCode AI as the hosted fallback. DBCode AI does not require an API-key setup screen. A Custom Model requires an OpenAI-compatible endpoint and model in DBCode AI settings, plus DBCode's Set Custom Model API Key command when the provider requires authentication.
- 2026-07-26: The source fix now preserves same-action drawer toggling, lets the topmost quick input or context menu consume Escape before an underlying drawer closes, and closes temporary drawers when focus enters a DBCode result-grid iframe. Focused shell, proof-state, same-Mac, feature-policy, host, release, update, and private-package source contracts pass. Ticket resolution still waits for the rebuilt rendered click, focus, keyboard, narrow-window, drawer, connection, and AI-provider checks.
- 2026-07-26: The user clarified that Database Explorer is persistent workspace navigation rather than a temporary drawer. A focused contract first failed because the shell had no persistent-drawer rule. The source now excludes only DBCode's Connections explorer from outside pointer and result-webview focus dismissal; its direct toolbar action and Escape still close it, and all other DBCode drawers keep their temporary behavior. The patch reapplies cleanly to pinned Code OSS. Rebuild and rendered verification remain required.
- 2026-07-26: The user finalized the persistence rule: outside clicks, DBCode result-grid focus, and Escape must not dismiss Database Explorer. Only its own toolbar action may toggle it closed, or another DBCode action may replace it in the shared sidebar. A focused contract failed against the old Escape behavior, passed after the handler was limited to temporary drawers, and the consolidated patch reapplied cleanly to pinned Code OSS. Rebuild and rendered verification remain required.
- 2026-07-26: The complete development source gate passes after the finalized persistence change. This covers the focused shell, patch plan, DBCode capability policy, profile isolation, Host Session, debugger fixture, notebooks, release-set compatibility, update, rollback, and public-source contracts. The installed app remains unchanged until it is fully quit, rebuilt, and checked through the rendered gate.
- 2026-07-26: The final patch-tree digest is bound into the semantic patch plan. The app was rebuilt, its refreshed source-set identity was embedded, the unchanged compiled app was re-signed after that identity refresh, the external manifest matches it, and static host smoke passed identity, architecture, SQL association, nested signature, renderer readiness, isolated launch, and normal-profile preservation.
- 2026-07-26: The first rendered run passed the real notebook and its human Kernel gate, the DBCode diagram route, outside-click persistence, and Escape beneath a context menu, then stopped in the new persistence helper because its state snapshot was incorrectly declared constant before a refresh. The helper was corrected and its source contract passes. A second full run reached the same human Kernel gate but timed out without approval, so the notebook cell never ran and the later persistence assertions were not reached. This is recorded as an incomplete human-gated run, not an application failure or a rendered pass. One attended full rendered run remains required.
- 2026-07-27: The Kernel gate is now a short attended preflight instead of a pause halfway through the full rendered suite. It writes a receipt bound to the exact app digest, release-set ID, build-manifest digest, and isolated QA profile layout. The full rendered command stops before launch when that receipt is absent or stale, and an unexpected returned prompt uses the normal rendered timeout rather than the five-minute human timeout. The duplicate extension-host log-policy runner was removed, both direct JavaScript checks now use pinned Node through their owning adapters, and the complete development source gate passed in 59.16 seconds. The preflight and one uninterrupted full rendered run remain required.
- 2026-07-27: Preflight diagnosis removed two unrelated blockers without weakening the human gate. A short disposable profile name avoids the macOS Electron path-length launch failure. The preflight now skips the wrapper's first-launch Profile Setup extension because runtime preparation is already complete and Profile Setup remains covered by the full rendered suite. It also asks for foreground focus before using the toolbar and bounds failure cleanup to its exact isolated child process. The latest run stopped safely because the visible window was not clicked within the five-minute attended window; it never reached or answered the Kernel prompt. An attended preflight and the following unattended full rendered run remain required.
- 2026-07-27: After the AI policy split, public documentation, runner cleanup, and preflight failure handling landed, the complete non-interactive development gate passed again in 60.42 seconds. It did not build or launch the app and did not require human input.
- 2026-07-27: The missed-window focus wait now has its own one-minute timeout. The five-minute allowance remains only for the actual first Kernel approval and startup, where macOS and Jupyter can legitimately need more time.
- 2026-07-27: Final receipt review found that the shell recorded the named QA layout while the attended check used a random disposable profile. The preflight now runs against the exact generated `qa` layout named in the receipt, including its cache and log roots. Starting a new preflight removes the prior receipt first, so a failed rerun or repeated prompt cannot leave older approval usable. The receipt identity, match, owner-only write, and invalidation rules now have focused behaviour tests.
- 2026-07-27: A deterministic command-interface test now runs the real rendered-check shell against local stubs. It proves that a new attended preflight removes an old receipt before child launch and that a stale receipt stops the full run before preparation or launch. The final unattended development gate passed in 58.49 seconds without rebuilding or opening the app. The real attended preflight and one uninterrupted rendered run remain required.
- 2026-07-27: The user chose a simpler deployment model. The attended Kernel preflight, permission receipt, disposable rendered profiles, and human waits have been removed from the maintained test path. The rendered smoke now reuses the single generated `qa` profile and checks DBCode routes without starting Python, executing SQL, calling a model, entering secrets, accepting terms, or approving macOS prompts. First-launch migration, recovery, live databases, licences, and permission choices are normal-use concerns or focused source contracts, not steps inside deployment automation. Earlier comments above remain as history of the superseded approach.
- 2026-07-27: The new one-profile smoke passed the prompt-free parts of this ticket: the release-status quick input stayed above the toolbar and dismissed on an outside click, Database Explorer stayed open across outside click and Escape until its toolbar toggle closed it, Connections Home stayed in the main canvas, and Account dismissed on an outside click. DBCode terms blocked notebook, Query Builder, settings, and AI command activation, so the default gate now records those rendered Tools routes as reachable without activating them. Context-menu behaviour, every temporary drawer type, and provider selection remain unverified here, so this ticket stays claimed rather than being marked resolved.
- 2026-07-27: Closed under the final prompt-free verification policy. The focused shell contract proves one shared implementation for quick inputs, context views, temporary drawers, persistent Database Explorer, webview focus, Escape, and outside clicks. The approved one-profile smoke supplies representative rendered evidence without accepting DBCode terms or calling a provider. `./script/test_focused_shell_contract.sh` and `./script/test_dbcode_contract.sh` passed without building or launching the app.

## Answer

Quick inputs and context menus stay open while used and close on an outside click or Escape. DBCode side drawers now remain persistent while the user works elsewhere; another DBCode view may replace the current one, and the shared drawer control collapses or restores it. Account is the only temporary drawer and still closes on an outside click or Escape.

Connections Home remains the primary connection route, and the adjacent tools menu no longer repeats it. DBCode Tools keeps clear routes for DBCode AI, Custom Model settings, and the DBCode-owned API-key command. Deployment verifies those routes without accepting terms, entering secrets, or making model calls.
